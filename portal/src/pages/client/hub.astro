---
import ClientLayout from '../../layouts/ClientLayout.astro';
import { siteLinks } from '../../lib/site-links';
---

<ClientLayout title="Centro de Cliente | Grande & Gordo">
  <div class="space-y-6" role="region" aria-label="Contenido principal">
    <section class="rounded-3xl border border-crm-border bg-crm-bg-alt p-6 md:p-8">
      <p class="text-xs font-bold uppercase tracking-[0.28em] text-crm-text-secondary">Centro de cliente</p>
      <h1 class="mt-4 text-3xl font-black tracking-tight text-crm-text md:text-5xl">Todo tu operativo, conectado</h1>
      <p class="mt-4 max-w-3xl text-sm leading-relaxed text-crm-muted md:text-base">
        Desde aquí puedes seguir todos tus trabajos, revisar entregables, enviar feedback y mantener el contexto ordenado.
      </p>
    </section>

    <!-- Estado Operativo -->
    <section class="rounded-2xl border border-crm-border bg-crm-bg-alt p-6 md:p-7">
      <h2 class="text-xl font-bold text-crm-text mb-4">Estado operativo</h2>
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-4 text-sm">
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Plan</p>
          <p id="hub-plan" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Dataset</p>
          <p id="hub-dataset" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Capacidad</p>
          <p id="hub-capacity" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Segmento</p>
          <p id="hub-segment" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Proxima review</p>
          <p id="hub-next-review" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Ultimo contacto</p>
          <p id="hub-last-contact" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4 md:col-span-2">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Contacto operativo</p>
          <p id="hub-account-manager" class="text-base font-semibold text-crm-text mt-1">hola@grandeandgordo.com</p>
        </div>
      </div>
    </section>

    <!-- Accesos Rápidos -->
    <section class="rounded-2xl border border-crm-border bg-crm-bg-alt p-6 md:p-7">
      <h2 class="text-xl font-bold text-crm-text mb-4">Accesos rápidos</h2>
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">
        <a href="/client" class="group rounded-xl border border-crm-border bg-crm-panel p-5 transition hover:border-crm-border">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-lg flex items-center justify-center" style="background-color: color-mix(in srgb, var(--crm-accent) 20%, transparent);">
              <svg class="w-5 h-5 text-crm-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
              </svg>
            </div>
            <div>
              <p class="text-sm font-semibold text-crm-text group-hover:text-crm-text">Resumen</p>
              <p class="text-xs text-crm-text-secondary mt-0.5">Resumen de trabajos</p>
            </div>
          </div>
        </a>

        <a href="/client/jobs" class="group rounded-xl border border-crm-border bg-crm-panel p-5 transition hover:border-crm-border">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-lg flex items-center justify-center" style="background-color: color-mix(in srgb, var(--crm-info) 20%, transparent);">
              <svg class="w-5 h-5 text-crm-info" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
              </svg>
            </div>
            <div>
              <p class="text-sm font-semibold text-crm-text group-hover:text-crm-text">Trabajos</p>
              <p class="text-xs text-crm-text-secondary mt-0.5">Todos tus jobs</p>
            </div>
          </div>
        </a>

        <a href="/client/assets" class="group rounded-xl border border-crm-border bg-crm-panel p-5 transition hover:border-crm-border">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-lg flex items-center justify-center" style="background-color: color-mix(in srgb, var(--crm-success) 20%, transparent);">
              <svg class="w-5 h-5 text-crm-success" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
              </svg>
            </div>
            <div>
              <p class="text-sm font-semibold text-crm-text group-hover:text-crm-text">Assets</p>
              <p class="text-xs text-crm-text-secondary mt-0.5">Todos tus entregables</p>
            </div>
          </div>
        </a>

        <a href={siteLinks.brief} target="_blank" rel="noreferrer" class="group rounded-xl border border-crm-border bg-crm-panel p-5 transition hover:border-crm-border">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-lg flex items-center justify-center" style="background-color: color-mix(in srgb, var(--crm-warning) 20%, transparent);">
              <svg class="w-5 h-5 text-crm-warning" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
              </svg>
            </div>
            <div>
              <p class="text-sm font-semibold text-crm-text group-hover:text-crm-text">Brief</p>
              <p class="text-xs text-crm-text-secondary mt-0.5">Actualizar contexto</p>
            </div>
          </div>
        </a>

        <a href="/client/onboarding" class="group rounded-xl border border-crm-border bg-crm-panel p-5 transition hover:border-crm-border">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-lg flex items-center justify-center" style="background-color: color-mix(in srgb, var(--crm-danger) 20%, transparent);">
              <svg class="w-5 h-5 text-crm-danger" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
            </div>
            <div>
              <p class="text-sm font-semibold text-crm-text group-hover:text-crm-text">Onboarding</p>
              <p class="text-xs text-crm-text-secondary mt-0.5">Checklist de sesión</p>
            </div>
          </div>
        </a>

        <a href="/client/profile" class="group rounded-xl border border-crm-border bg-crm-panel p-5 transition hover:border-crm-border">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-lg bg-crm-accent-soft flex items-center justify-center">
              <svg class="w-5 h-5 text-crm-muted" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
              </svg>
            </div>
            <div>
              <p class="text-sm font-semibold text-crm-text group-hover:text-crm-text">Perfil</p>
              <p class="text-xs text-crm-text-secondary mt-0.5">Tu cuenta y datos</p>
            </div>
          </div>
        </a>
      </div>
    </section>

    <!-- Últimos Trabajos -->
    <section class="rounded-2xl border border-crm-border bg-crm-bg-alt p-6 md:p-7">
      <div class="flex items-center justify-between mb-4">
        <div>
          <h2 class="text-xl font-bold text-crm-text">Últimos trabajos</h2>
          <p class="text-sm text-crm-muted mt-1">Sigue el estado de tus jobs y entregables</p>
        </div>
        <a href="/client/jobs" class="text-sm font-semibold text-crm-accent hover:text-crm-text transition">Ver todos →</a>
      </div>
      <div class="overflow-x-auto">
        <table class="min-w-full text-sm">
          <thead class="bg-crm-panel">
            <tr>
              <th class="px-4 py-3 text-left text-xs font-bold uppercase tracking-[0.2em] text-crm-text-secondary">Brief</th>
              <th class="px-4 py-3 text-left text-xs font-bold uppercase tracking-[0.2em] text-crm-text-secondary">Estado</th>
              <th class="px-4 py-3 text-left text-xs font-bold uppercase tracking-[0.2em] text-crm-text-secondary">Due</th>
              <th class="px-4 py-3 text-left text-xs font-bold uppercase tracking-[0.2em] text-crm-text-secondary">Units</th>
              <th class="px-4 py-3"></th>
            </tr>
          </thead>
          <tbody id="hub-jobs-body" class="divide-y divide-crm-border">
            <tr>
              <td colspan="5" class="px-4 py-6 text-center text-crm-text-secondary">Cargando trabajos...</td>
            </tr>
          </tbody>
        </table>
      </div>
    </section>

    <!-- Últimos Assets -->
    <section class="rounded-2xl border border-crm-border bg-crm-bg-alt p-6 md:p-7">
      <div class="flex items-center justify-between mb-4">
        <div>
          <h2 class="text-xl font-bold text-crm-text">Últimos assets</h2>
          <p class="text-sm text-crm-muted mt-1">Tus entregables aprobados</p>
        </div>
        <a href="/client/assets" class="text-sm font-semibold text-crm-accent hover:text-crm-text transition">Ver todos →</a>
      </div>
      <div id="hub-assets-grid" class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">
        <p class="text-crm-text-secondary">Cargando assets...</p>
      </div>
    </section>
  </div>

  <script>
    (async () => {
      const {
        api,
        escapeHtml,
        formatDate,
        formatNumber,
        getSessionContext,
      } = await import('../../lib/api-client');
      const { datasetStatusClass, jobStatusClass, subscriptionClass } = await import('../../lib/ui-helpers');

      const setText = (id, value) => {
        const element = document.getElementById(id);
        if (element) element.textContent = value;
      };

      try {
        const [session, jobsData, assetsData] = await Promise.all([
          getSessionContext(),
          api('/api/portal/jobs'),
          api('/api/portal/assets').catch(() => ({ assets: [] })),
        ]);

        const client = session?.client;
        const jobs = Array.isArray(jobsData.jobs) ? jobsData.jobs : [];
        const assets = Array.isArray(assetsData.assets) ? assetsData.assets : [];

        // Estado operativo
        if (client) {
          setText('hub-plan', client.plan ?? 'Sin plan definido');
          setText('hub-dataset', client.datasetStatus ?? 'pending_capture');
          setText('hub-capacity', `${formatNumber(client.monthlyUnitCapacity ?? 0)} unidades`);
          setText('hub-segment', `${client.segment ?? 'Sin segmento'} / ${client.marginProfile ?? 'Sin margen'}`);
          setText('hub-next-review', formatDate(client.nextReviewAt));
          setText('hub-last-contact', formatDate(client.lastContactedAt));
          setText('hub-account-manager', client.accountManager ?? 'hola@grandeandgordo.com');

          const datasetEl = document.getElementById('hub-dataset');
          if (datasetEl) {
            datasetEl.className = `text-base font-semibold mt-1 ${datasetStatusClass(client.datasetStatus ?? '')}`;
          }

          const planEl = document.getElementById('hub-plan');
          if (planEl) {
            planEl.className = `text-base font-semibold mt-1 ${subscriptionClass(client.subscriptionStatus ?? 'inactive')}`;
          }
        }

        // Últimos trabajos (top 5)
        const jobsBody = document.getElementById('hub-jobs-body');
        if (jobsBody) {
          if (jobs.length === 0) {
            jobsBody.innerHTML = `
              <tr>
                <td colspan="5" class="px-4 py-6 text-center text-crm-text-secondary">Todavía no tienes trabajos activos.</td>
              </tr>
            `;
          } else {
            const recentJobs = jobs.slice(0, 5);
            jobsBody.innerHTML = recentJobs.map((job) => `
              <tr class="hover:bg-crm-panel-soft transition">
                <td class="px-4 py-3">
                  <p class="font-medium text-crm-text">${escapeHtml(job.briefText ?? 'Sin brief')}</p>
                  <p class="text-xs text-crm-text-secondary mt-0.5">${escapeHtml(job.platform ?? 'Sin plataforma')} · ${escapeHtml(job.type ?? 'Sin tipo')}</p>
                </td>
                <td class="px-4 py-3">
                  <span class="inline-flex px-2 py-1 rounded text-xs font-medium ${jobStatusClass(job.status ?? '')}">
                    ${escapeHtml(job.status ?? 'pending')}
                  </span>
                </td>
                <td class="px-4 py-3 text-crm-muted">${escapeHtml(formatDate(job.dueAt))}</td>
                <td class="px-4 py-3 text-crm-muted">${escapeHtml(formatNumber(job.unitsConsumed ?? 0))} / ${escapeHtml(formatNumber(job.unitsPlanned ?? 0))}</td>
                <td class="px-4 py-3 text-right">
                  <a href="/client/jobs/detail?id=${encodeURIComponent(job.id)}" class="font-medium text-crm-text underline decoration-crm-border underline-offset-4 transition hover:decoration-crm-text">Ver</a>
                </td>
              </tr>
            `).join('');
          }
        }

        // Últimos assets (top 6)
        const assetsGrid = document.getElementById('hub-assets-grid');
        if (assetsGrid) {
          if (assets.length === 0) {
            assetsGrid.innerHTML = '<p class="text-crm-text-secondary col-span-full">Todavía no hay assets aprobados.</p>';
          } else {
            const recentAssets = assets.slice(0, 6);
            assetsGrid.innerHTML = recentAssets.map((asset) => `
              <article class="rounded-xl border border-crm-border bg-crm-panel p-4">
                <p class="text-sm font-semibold text-crm-text">${escapeHtml(asset.label ?? asset.type)}</p>
                <p class="text-xs text-crm-text-secondary mt-1">${escapeHtml(asset.type ?? 'asset')}</p>
                ${
                  asset.deliveryUrl
                    ? `<a href="${escapeHtml(asset.deliveryUrl)}" target="_blank" rel="noreferrer" class="inline-block mt-3 text-xs font-semibold text-crm-accent hover:text-crm-text transition">Abrir asset →</a>`
                    : '<p class="text-xs text-crm-text-secondary mt-3">Disponible pronto</p>'
                }
              </article>
            `).join('');
          }
        }
      } catch (error) {
        console.error('Hub loading error:', error);
      }
    })();
  </script>
</ClientLayout>