---
import ClientLayout from '../../layouts/ClientLayout.astro';
import { siteLinks } from '../../lib/site-links';
---

<ClientLayout title="Perfil">
  <div class="max-w-5xl space-y-6" role="region" aria-label="Contenido principal">
    <section class="rounded-3xl border border-crm-border bg-crm-bg-alt p-6 md:p-8">
      <p class="text-xs font-bold uppercase tracking-[0.28em] text-crm-text-secondary">Perfil cliente</p>
      <h1 class="mt-4 text-3xl font-black tracking-tight text-crm-text md:text-5xl">Mi perfil</h1>
      <p class="mt-4 max-w-3xl text-sm leading-relaxed text-crm-muted md:text-base">
        Gestiona tu cuenta de acceso y revisa el contexto operativo de tu relacion con
        Grande &amp; Gordo desde una sola vista.
      </p>
    </section>

    <div class="grid gap-6 lg:grid-cols-[1.15fr_.85fr]">
      <section class="rounded-2xl border border-crm-border bg-crm-bg-alt p-6 md:p-7">
        <h2 class="text-lg font-semibold text-crm-text mb-4">Cuenta</h2>
        <dl class="grid sm:grid-cols-2 gap-4 text-sm">
          <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
            <dt class="text-crm-text-secondary text-xs uppercase tracking-wider">Nombre</dt>
            <dd id="profile-name" class="mt-1 text-base font-semibold text-crm-text">Cargando...</dd>
          </div>
          <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
            <dt class="text-crm-text-secondary text-xs uppercase tracking-wider">Email</dt>
            <dd id="profile-email" class="mt-1 text-base font-semibold text-crm-text">—</dd>
          </div>
          <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
            <dt class="text-crm-text-secondary text-xs uppercase tracking-wider">Empresa</dt>
            <dd id="profile-company" class="mt-1 text-base font-semibold text-crm-text">—</dd>
          </div>
          <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
            <dt class="text-crm-text-secondary text-xs uppercase tracking-wider">Contacto operativo</dt>
            <dd id="profile-account-manager" class="mt-1 text-base font-semibold text-crm-text">hola@grandeandgordo.com</dd>
          </div>
        </dl>
      </section>

      <section class="rounded-2xl border border-crm-border bg-crm-bg-alt p-6 md:p-7">
        <h2 class="text-lg font-semibold text-crm-text mb-4">Cambiar contrasena</h2>
        <div id="password-success" class="hidden crm-alert crm-alert-success mb-4"></div>
        <div id="password-error" class="hidden crm-alert crm-alert-danger mb-4"></div>

        <form id="password-form" class="space-y-4">
          <div>
            <label for="currentPassword" class="mb-2 block text-sm font-medium text-crm-text-secondary">Contrasena actual</label>
            <input type="password" id="currentPassword" name="currentPassword" required class="crm-input w-full" />
          </div>
          <div>
            <label for="newPassword" class="mb-2 block text-sm font-medium text-crm-text-secondary">Nueva contrasena</label>
            <input type="password" id="newPassword" name="newPassword" required minlength="8" class="crm-input w-full" />
          </div>
          <button type="submit" class="crm-button crm-button-primary w-full">
            Actualizar contrasena
          </button>
        </form>
      </section>
    </div>

    <section class="rounded-2xl border border-crm-border bg-crm-bg-alt p-6 md:p-7">
      <h2 class="text-lg font-semibold text-crm-text mb-4">Estado operativo</h2>
      <div class="grid md:grid-cols-2 xl:grid-cols-4 gap-4 text-sm">
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Plan</p>
          <p id="profile-plan" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Dataset</p>
          <p id="profile-dataset" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Capacidad mensual</p>
          <p id="profile-capacity" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Segmento / margen</p>
          <p id="profile-segment" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Proxima review</p>
          <p id="profile-next-review" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Ultimo contacto</p>
          <p id="profile-last-contact" class="text-base font-semibold text-crm-text mt-1">—</p>
        </div>
        <div class="rounded-xl border border-crm-border bg-crm-panel p-4 md:col-span-2">
          <p class="text-crm-text-secondary text-xs uppercase tracking-wider">Notas compartidas</p>
          <p id="profile-notes" class="text-base text-crm-text-secondary mt-1">—</p>
        </div>
      </div>
    </section>

    <section class="rounded-2xl border border-crm-border bg-crm-bg-alt p-6 md:p-7">
      <div class="flex flex-col gap-3 md:flex-row md:items-start md:justify-between">
        <div>
          <p class="text-xs font-bold uppercase tracking-[0.28em] text-crm-text-secondary">Continuidad</p>
          <h2 class="mt-3 text-2xl font-black tracking-tight text-crm-text">Ultimo brief recibido</h2>
          <p id="profile-brief-meta" class="text-sm text-crm-text-secondary mt-3">
            Aun no tenemos un brief reciente asociado a tu cuenta.
          </p>
          <p id="profile-brief-description" class="text-sm text-crm-muted mt-4 leading-relaxed">
            Si quieres actualizar contexto, referencias o necesidades, puedes hacerlo desde la web y quedará conectado con tu operativa del CRM.
          </p>
        </div>
        <a
          href={siteLinks.brief}
          target="_blank"
          rel="noreferrer"
          class="crm-button crm-button-primary"
        >
          Actualizar brief
        </a>
      </div>
    </section>
  </div>

  <script>
    (async () => {
      const {
        api,
        API_BASE,
        formatDate,
        formatNumber,
        getSessionContext,
      } = await import('../../lib/api-client');

      const [session, briefData] = await Promise.all([
        getSessionContext(),
        api('/api/portal/briefs/latest').catch(() => ({ brief: null })),
      ]);
      const user = session?.user;
      const client = session?.client;
      const latestBrief = briefData?.brief ?? null;

      const setText = (id, value) => {
        const element = document.getElementById(id);
        if (element) element.textContent = value;
      };

      if (user) {
        setText('profile-name', user.name);
        setText('profile-email', user.email);
        setText('profile-company', user.company ?? client?.company ?? 'Grande&Gordo');
      }

      if (client) {
        setText('profile-account-manager', client.accountManager ?? 'hola@grandeandgordo.com');
        setText('profile-plan', client.plan ?? 'Sin plan definido');
        setText('profile-dataset', client.datasetStatus ?? 'pending_capture');
        setText('profile-capacity', `${formatNumber(client.monthlyUnitCapacity ?? 0)} unidades`);
        setText('profile-segment', `${client.segment ?? 'Sin segmento'} / ${client.marginProfile ?? 'Sin margen'}`);
        setText('profile-next-review', formatDate(client.nextReviewAt));
        setText('profile-last-contact', formatDate(client.lastContactedAt));
        setText('profile-notes', client.notes ?? 'Sin notas compartidas por ahora.');
      }

      const profileBriefMeta = document.getElementById('profile-brief-meta');
      const profileBriefDescription = document.getElementById('profile-brief-description');
      if (latestBrief) {
        if (profileBriefMeta) {
          profileBriefMeta.textContent =
            `${latestBrief.tipo ?? 'brief'} · recibido ${formatDate(latestBrief.createdAt)} · estado ${latestBrief.status ?? 'new'}`;
        }

        if (profileBriefDescription) {
          const excerpt = String(latestBrief.description ?? '').trim();
          profileBriefDescription.textContent =
            excerpt.length > 280 ? `${excerpt.slice(0, 277)}...` : excerpt || 'Brief recibido correctamente.';
        }
      }

      const form = document.getElementById('password-form');
      const errorDiv = document.getElementById('password-error');
      const successDiv = document.getElementById('password-success');

      form?.addEventListener('submit', async (event) => {
        event.preventDefault();
        errorDiv?.classList.add('hidden');
        successDiv?.classList.add('hidden');

        const currentPassword = document.getElementById('currentPassword');
        const newPassword = document.getElementById('newPassword');

        try {
          const res = await fetch(`${API_BASE}/api/portal/auth/change-password`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            credentials: 'include',
            body: JSON.stringify({
              currentPassword:
                currentPassword instanceof HTMLInputElement ? currentPassword.value : '',
              newPassword:
                newPassword instanceof HTMLInputElement ? newPassword.value : '',
            }),
          });

          if (!res.ok) {
            const data = await res.json().catch(() => ({}));
            throw new Error(data.error ?? 'No se pudo cambiar la contrasena');
          }

          if (form instanceof HTMLFormElement) {
            form.reset();
          }

          if (successDiv instanceof HTMLDivElement) {
            successDiv.textContent = 'Contrasena actualizada correctamente.';
            successDiv.classList.remove('hidden');
          }
        } catch (error) {
          if (errorDiv instanceof HTMLDivElement) {
            errorDiv.textContent =
              error instanceof Error ? error.message : 'No se pudo cambiar la contrasena';
            errorDiv.classList.remove('hidden');
          }
        }
      });
    })();
  </script>
</ClientLayout>
